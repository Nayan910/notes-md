# Session 003: Android SDK Setup, APK Build & Environment

**Date:** 2026-05-16
**Bob:** Current (OpenCode)
**Duration:** ~2 hours
**State at start:** Phase 1-3 code existed. Android SDK not installed. JDK 25 present but incompatible with Kotlin/Gradle. APK build blocked.

## What Was Done

### Android SDK Setup
- Downloaded cmdline-tools to `E:\apps\android-sdk\cmdline-tools\latest\`
- Installed platforms android-35, android-36 + build-tools 35.0.0, 36.0.0 + platform-tools
- Initial sdkmanager --licenses hung on "y" prompt; killed and accepted via repeated runs
- NDK 28.2.13676358 auto-installed by Gradle

### JDK 25 → JDK 21 Migration
- JDK 25 (C:\Program Files\Java\jdk-25) caused Kotlin compiler to crash:
  ```
  java.lang.IllegalArgumentException: 25.0.1
  at org.jetbrains.kotlin.com.intellij.util.lang.JavaVersion.parse
  ```
- Installed Adoptium JDK 21 via `winget` to `E:\apps\jdk-21`
- Gradle 8.14 works with JDK 21

### Cache Relocation (C: → E:)
- Flutter cache at `C:\Users\nayan\flutter\bin\cache` (~735MB) moved to `E:\apps\flutter-cache`
- Symlink created: `cache → E:\apps\flutter-cache` (Admin: symlink creation succeeded)
- `PUB_CACHE` set to `E:\apps\pub-cache`

### File Upload Black Screen Bug
- Earlier attempted fix: added `onFileChooser` callback with `FileChooserAction.NATIVE`
- **Issue:** `flutter_inappwebview` (v6.1.5) does NOT have `onFileChooser` parameter
- Reverted the `onFileChooser` callback
- Added storage permissions to `AndroidManifest.xml`
- **Status:** File upload black screen not yet fixed. Need JS bridge approach (intercept file inputs → Flutter file_picker → send base64 back)

### Debug APK Built Successfully
- `E:\oprncode\project\apps\notes-md-app\build\app\outputs\flutter-apk\app-debug.apk`
- Copied to `E:\oprncode\project\notes-md-debug.apk` (163MB)
- Build time: ~8 minutes (first-time Gradle dependency downloads)

### Vite Frontend Server
- Started on `localhost:5173` (verified listening)
- Backend server not started in this session

### IP Addresses Updated
- `home_screen.dart`: `192.168.1.100` → `192.168.1.8`
- `pairing.py`: `localhost:8000` → `192.168.1.8:8000` (need backend running for mobile testing)

## Key Decisions
1. **JDK 21 over JDK 25:** JDK 25 Kotlin parser doesn't support "25.0.1" version string
2. **Cache on E: drive:** Symlinked to conserve C: space (~14GB free)
3. **Pandoc for document conversion:** User chose pandoc approach for importing/exporting .docx/.odt/.html/.txt ↔ .md
4. **onFileChooser removed:** v6.1.5 doesn't have it; will use JS bridge approach for file upload fix

## Blocked / Questions
- **File upload black screen:** Needs JS bridge → Flutter file_picker → base64 → WebView injection
- **Backend not started:** `cd backend\notes-md-api && python main.py` needed for pairing/API
- **Windows build:** Blocked by no Visual Studio
- **Vite dev vs production:** APK loads `192.168.1.8:5173` (dev). Production needs built files.

## Priorities for Next Bob
1. Fix file upload black screen (JS bridge approach)
2. Implement document conversion (pandoc backend endpoint + Flutter UI)
3. Start backend server for end-to-end testing
4. Build production web bundle for Flutter to serve locally
5. Phase 4 P2P sync research

## Files Changed
```
MODIFIED:
  apps/notes-md-app/lib/screens/home_screen.dart — removed onFileChooser, updated IP
  apps/notes-md-app/android/app/src/main/AndroidManifest.xml — added INTERNET + READ_EXTERNAL_STORAGE
  backend/notes-md-api/pairing.py — updated server URL to 192.168.1.8:8000

CREATED:
  .memory/sessions/003-2026-05-16-session3.md

SYSTEM CHANGES:
  E:\apps\android-sdk\ — SDK installed (platform 35+36, build-tools, platform-tools)
  E:\apps\jdk-21\ — JDK 21 installed from Adoptium
  E:\apps\flutter-cache\ — Flutter cache symlinked from C: drive
  E:\oprncode\project\notes-md-debug.apk — Debug APK (163MB)
```
