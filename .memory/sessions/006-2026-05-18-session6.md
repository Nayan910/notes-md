# Session 006: APK Build + Windows Prep + Flutter Login/Standalone

**Date:** 2026-05-18
**State at start:** Session 005 complete. Feature code done. 4 commits ready. APK stale (May 16).
**Mode:** Hands-on with user present

## Tasks

### Task 1: Flutter Login Screen + Standalone Mode ✅
- Created `login_screen.dart` — username/password login/register
- "Use Offline" button — navigates to `/editor` without any auth (standalone)
- QR pairing still accessible via small link
- Updated `auth_service.dart` — added `login()` and `register()` methods
- Updated `main.dart` — loading → login → home/editor flow
- Updated `home_screen.dart` — logout redirects to `/login`
- **Commit:** `d1c0871`

### Task 2: Android APK Build ✅ (177 MB)
- Android SDK found at `E:\apps\android-sdk`
- Flutter SDK found at `C:\Users\nayan\flutter` (not in PATH)
- SDK licenses accepted
- Web dist rebuilt (913 modules)
- APK rebuilt successfully (took 488s)
- Location: `E:\oprncode\project\notes-md-debug.apk`

### Task 3: VS Build Tools Download ✅
- Downloaded VS Build Tools installer (4.3 MB) to `E:\apps\vs-buildtools\`
- Installation failed — requires admin rights
- **Needs:** Right-click `vs_BuildTools.exe` → Run as Administrator, select "Desktop development with C++" workload

### Task 4: Push to Codeberg ❌
- 4 commits ahead: `d1c0871`, `13846d0`, `fa4e72f`, `9712c5c`
- Credentials expired — needs fresh PAT/token

## Current State

| Item | Status |
|------|--------|
| Android APK (`notes-md-debug.apk`) | ✅ 177 MB, with login + standalone |
| Windows app | 🔴 Needs VS Build Tools + admin install |
| Push to Codeberg | 🔴 Needs fresh credentials |
| VS Build Tools installer | ✅ Downloaded, needs admin run |

## Files Changed

| File | Change |
|------|--------|
| `apps/notes-md-app/lib/screens/login_screen.dart` | New — login/register + standalone mode |
| `apps/notes-md-app/lib/services/auth_service.dart` | Added `login()`, `register()` methods |
| `apps/notes-md-app/lib/main.dart` | Routes: loading → login → home/editor |
| `apps/notes-md-app/lib/screens/home_screen.dart` | Logout → `/login` |
| `E:\apps\vs-buildtools\vs_BuildTools.exe` | VS Build Tools installer |
| `build-apk.bat` | APK build script (double-click) |
| `build-all.bat` | APK + Windows build script |
| `E:\oprncode\project\notes-md-debug.apk` | Latest APK (177 MB, login + standalone) |

### Late additions (same session):
- Alpha release `v0.1.0-alpha` created at https://codeberg.org/nayanchotaliya/notes-md/releases/tag/v0.1.0-alpha
  - Needed `has_releases=true` via PATCH API first (was `false` by default)
- Commit `c805ae6` — session 6 + build scripts + pushed to Codeberg
- VS Build Tools installer downloaded to `E:\apps\vs-buildtools\` (pending admin install)
